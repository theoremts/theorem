# @theoremts/core

Core engine for [Theorem](https://github.com/theoremts/theorem) — parser, translator, Z3 solver, scanner, and suggester.

Used internally by `theoremts-cli` and `theoremts-ts-plugin`. Install those instead unless you're building tools on top of Theorem.

```bash
npm install @theoremts/core
```
